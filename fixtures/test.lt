inductive Boolean : Type :=
      True : Boolean
    | False : Boolean;

def hello : Boolean := {}

def hello_world (hello : Boolean) (world : Boolean) : Boolean := {}

def foo : Boolean := True

def world : Boolean := 5

def stmts : Boolean := {
    let x := 5;
    let y := 10;
    x := 10;
}